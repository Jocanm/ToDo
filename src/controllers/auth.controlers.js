export const refreshToken = (req,res) => {

    res.send("Ruta para refrescar el token")

}


export const createUser = (req, res) => {

    res.send("Ruta para crear un usuario")

}

export const loginUser = (req, res) => {

    res.send("Ruta para logear un usuario")

}